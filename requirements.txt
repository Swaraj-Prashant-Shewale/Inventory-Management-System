PySide6==6.10.2; sys_platform == "win32"
peewee==4.0.0
psycopg2-binary==2.9.11
python-dotenv==1.2.1

# Documents and export
reportlab
openpyxl
python-barcode
Pillow

# Web application
fastapi
uvicorn[standard]
jinja2
python-multipart
itsdangerous
httpx
qrcode          # optional: draws the admin-2fa enrolment QR in the terminal

# Packaging (desktop build, being retired once the web app is live)
pyinstaller==6.19.0; sys_platform == "win32"
